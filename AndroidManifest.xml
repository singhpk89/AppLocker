<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twinone.locker"
    android:versionCode="202"
    android:versionName="2.0.2" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="com.twinone.locker.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Derived activities will return to MainActivity -->
        <activity
            android:name="com.twinone.locker.DieLockActivity"
            android:configChanges="orientation|screenSize"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.ChangePasswordActivity"
            android:configChanges="orientation|screenSize"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.SelectActivity"
            android:label="@string/applist_tit"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.PrefsActivity"
            android:label="@string/preferences_tit"
            android:noHistory="true" >
        </activity>
        <!-- End derived activities -->


        <!-- ExcludeFromRecents will hide it in task switcher -->
        <!-- NoHistory will finish in onPause -->
        <activity
            android:name="com.twinone.locker.LockActivity"
            android:configChanges="orientation|screenSize"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.StagingActivity"
            android:theme="@style/Theme_Transparent" >
        </activity>

        <service android:name="com.twinone.locker.ObserverService" />

        <receiver android:name="com.twinone.locker.BootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>