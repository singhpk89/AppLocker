<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twinone.locker"
    android:versionCode="206"
    android:versionName="2.0.6" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <!-- v1.0.0 -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <!-- v2.1.0 -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/application_name" >
        <activity
            android:name="com.twinone.locker.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/launcher_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="com.twinone.locker.MainActivityAlias"
            android:enabled="true"
            android:label="@string/launcher_name"
            android:targetActivity="com.twinone.locker.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Derived activities will return to MainActivity -->
        <activity
            android:name="com.twinone.locker.LockOwnActivity"
            android:configChanges="orientation|screenSize"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.prefs.ChangePasswordActivity"
            android:configChanges="orientation|screenSize"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.prefs.SelectActivity"
            android:label="@string/applist_tit"
            android:noHistory="true"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.prefs.PrefsActivity"
            android:label="@string/preferences_tit"
            android:noHistory="true" >
        </activity>
        <!-- End derived activities -->


        <!-- ExcludeFromRecents will hide it in task switcher -->
        <!-- NoHistory will finish in onPause -->
        <activity
            android:name="com.twinone.locker.LockActivity"
            android:configChanges="orientation|screenSize"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="com.twinone.locker.StagingActivity"
            android:theme="@style/Theme_Transparent" >
        </activity>

        <service android:name="com.twinone.locker.ObserverService" />

        <receiver android:name="com.twinone.locker.EventReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
            </intent-filter>
        </receiver>
    </application>

</manifest>